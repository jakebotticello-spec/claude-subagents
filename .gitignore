# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- editors ---
.vscode/
.idea/
*.swp
*~

# --- node ---
node_modules/
dist/
build/
npm-debug.log*
yarn-error.log*

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# --- secrets: never, under any circumstances ---
.env
.env.*
*.pem
*.key
id_rsa
id_ed25519
*.credentials.json

# --- scratch / transient ---
_scratch/
*.log
*.bak
*.tmp
.scribe-probe.txt

# --- generated / daemon-owned (CC call, S1 — see commit report) ---
# Filer-owned, rewritten continuously while the daemon runs. Observed changing
# mid-session (last_write_utc 17:25:20Z) when it picked up .claude/relay-arc.json.
# Tracking derived state a daemon rewrites means the working tree is never clean.
.canon-manifest.json

# --- canon / context-tracking apparatus (Jake's call, S1) ---
# Internal session-continuity machinery. This repo is PUBLIC and exists to point
# at the subagent work, not to publish the operating apparatus.
canon/BOOT.md
canon/changelog/
canon/handoffs/
canon/foundation/
canon/graveyard/

# --- MCP / relay / local tooling ---
.claude/
.codex/
